import { ProxyAction } from "~/.server/actions/proxy";
import { ProxyLoader } from "~/.server/loaders/proxy";

export const action = ProxyAction;
export const clientAction = ProxyAction;
export const loader = ProxyLoader;
